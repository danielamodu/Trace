# Secrets — never commit
.env
.env.local
*.key

# Node
node_modules/
npm-debug.log*

# Next.js / TypeScript build output
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Recon output that may contain unsanitized data
fixtures/raw/

# Saved live cases (local library, per-install; not part of the shipped set)
data/cases/

# Playwright e2e artifacts
test-results/
playwright-report/
blob-report/
/playwright/.cache/

# OS
.DS_Store
Thumbs.db
